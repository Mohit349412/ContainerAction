on: [push]

jobs:
  my-job:
    runs-on: ubuntu-latest
    
steps:
 -uses: actions/checkout@V2
 -name: "run the action"
id: hello
uses: ./
with:
myInput: 'Mohit Singh'
-name: output
# run: |
echo ${{ steps.hello.outputs.memory}}

